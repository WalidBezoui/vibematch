{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User Profile",
      "type": "object",
      "description": "Represents a user's public and private profile information.",
      "properties": {
        "uid": {
          "type": "string",
          "description": "The user's unique ID from Firebase Authentication."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "name": {
          "type": "string",
          "description": "The user's full name or company name."
        },
        "role": {
          "type": "string",
          "enum": [
            "brand",
            "creator"
          ],
          "description": "The role of the user, determining their permissions and view."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of when the user profile was created."
        }
      },
      "required": [
        "uid",
        "email",
        "name",
        "role",
        "createdAt"
      ]
    },
    "Campaign": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Campaign",
      "type": "object",
      "description": "Represents a collaboration contract between a Brand and a Creator.",
      "properties": {
        "title": {
          "type": "string",
          "description": "The title of the campaign."
        },
        "campaignBrief": {
          "type": "string",
          "description": "Detailed description of the campaign, goals, and requirements."
        },
        "deliverables": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "A list of specific deliverables required from the creator."
        },
        "price": {
          "type": "number",
          "description": "The payment amount for the job in Moroccan Dirhams (DH)."
        },
        "brandId": {
          "type": "string",
          "description": "The UID of the Brand who created the campaign."
        },
        "creatorId": {
          "type": "string",
          "description": "The UID of the Creator assigned to the campaign."
        },
        "status": {
          "type": "string",
          "enum": [
            "OPEN_FOR_APPLICATIONS",
            "PENDING_SELECTION",
            "PENDING_CREATOR_ACCEPTANCE",
            "PENDING_PAYMENT",
            "IN_PROGRESS",
            "DELIVERED",
            "COMPLETED",
            "REJECTED_BY_CREATOR"
          ],
          "description": "The current status of the campaign workflow."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": [
        "title",
        "campaignBrief",
        "deliverables",
        "price",
        "brandId",
        "status",
        "createdAt",
        "updatedAt"
      ]
    },
    "CampaignApplication": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Campaign Application",
      "type": "object",
      "description": "Represents a Creator's application to a specific campaign.",
      "properties": {
        "campaignId": {
          "type": "string",
          "description": "The ID of the campaign this application is for."
        },
        "creatorId": {
          "type": "string",
          "description": "The UID of the Creator who is applying."
        },
        "brandId": {
          "type": "string",
          "description": "The UID of the Brand who owns the campaign."
        },
        "coverLetter": {
          "type": "string",
          "description": "The creator's message to the brand for this application."
        },
        "status": {
          "type": "string",
          "enum": [
            "pending",
            "accepted",
            "rejected"
          ],
          "description": "The status of the application."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": [
        "campaignId",
        "creatorId",
        "brandId",
        "coverLetter",
        "status",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/entities/UserProfile"
          },
          "description": "Stores public and private information for each user.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the user."
            }
          ]
        }
      },
      {
        "path": "/campaigns/{campaignId}",
        "definition": {
          "entityName": "Campaign",
          "schema": {
            "$ref": "#/entities/Campaign"
          },
          "description": "Stores all campaign contracts between brands and creators.",
          "params": [
            {
              "name": "campaignId",
              "description": "The unique ID for the campaign."
            }
          ]
        },
        "subcollections": [
          {
            "path": "/campaigns/{campaignId}/applications/{applicationId}",
            "definition": {
              "entityName": "CampaignApplication",
              "schema": {
                "$ref": "#/entities/CampaignApplication"
              },
              "description": "Stores applications from creators for a specific campaign.",
              "params": [
                {
                  "name": "campaignId",
                  "description": "The unique ID of the parent campaign."
                },
                {
                  "name": "applicationId",
                  "description": "The unique ID for the application."
                }
              ]
            }
          }
        ]
      }
    ]
  }
}
