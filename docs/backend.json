{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User Profile",
      "type": "object",
      "description": "Represents a user's public and private profile information.",
      "properties": {
        "uid": {
          "type": "string",
          "description": "The user's unique ID from Firebase Authentication."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "name": {
          "type": "string",
          "description": "The user's full name or company name."
        },
        "role": {
          "type": "string",
          "enum": [
            "brand",
            "creator"
          ],
          "description": "The role of the user, determining their permissions and view."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of when the user profile was created."
        }
      },
      "required": [
        "uid",
        "email",
        "name",
        "role",
        "createdAt"
      ]
    },
    "Job": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Job",
      "type": "object",
      "description": "Represents a collaboration contract between a Brand and a Creator.",
      "properties": {
        "title": {
          "type": "string",
          "description": "The title of the campaign."
        },
        "campaignBrief": {
          "type": "string",
          "description": "Detailed description of the campaign, goals, and requirements."
        },
        "deliverables": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "A list of specific deliverables required from the creator."
        },
        "price": {
          "type": "number",
          "description": "The payment amount for the job in Moroccan Dirhams (DH)."
        },
        "brandId": {
          "type": "string",
          "description": "The UID of the Brand who created the job."
        },
        "creatorId": {
          "type": "string",
          "description": "The UID of the Creator assigned to the job."
        },
        "creatorEmail": {
          "type": "string",
          "format": "email",
          "description": "The email of the creator who is invited to the job."
        },
        "status": {
          "type": "string",
          "enum": [
            "PENDING_CREATOR_APPROVAL",
            "PENDING_PAYMENT",
            "IN_PROGRESS",
            "DELIVERED",
            "COMPLETED",
            "REJECTED"
          ],
          "description": "The current status of the job workflow."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        },
        "inviteId": {
          "type": "string",
          "description": "The ID of the associated job invitation."
        }
      },
      "required": [
        "title",
        "campaignBrief",
        "deliverables",
        "price",
        "brandId",
        "creatorEmail",
        "status",
        "createdAt",
        "updatedAt"
      ]
    },
    "JobInvite": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Job Invite",
      "type": "object",
      "description": "Represents a unique, one-time invitation for a Creator to accept a Job.",
      "properties": {
        "jobId": {
          "type": "string",
          "description": "The ID of the job this invitation is for."
        },
        "creatorEmail": {
          "type": "string",
          "format": "email",
          "description": "The email of the creator the invitation was sent to."
        },
        "status": {
          "type": "string",
          "enum": [
            "pending",
            "accepted",
            "expired"
          ],
          "description": "The status of the invitation."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "expiresAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": [
        "jobId",
        "creatorEmail",
        "status",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/entities/UserProfile"
          },
          "description": "Stores public and private information for each user.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the user."
            }
          ]
        }
      },
      {
        "path": "/jobs/{jobId}",
        "definition": {
          "entityName": "Job",
          "schema": {
            "$ref": "#/entities/Job"
          },
          "description": "Stores all job contracts between brands and creators.",
          "params": [
            {
              "name": "jobId",
              "description": "The unique ID for the job."
            }
          ]
        }
      },
      {
        "path": "/invites/{inviteId}",
        "definition": {
          "entityName": "JobInvite",
          "schema": {
            "$ref": "#/entities/JobInvite"
          },
          "description": "Stores unique invitation tokens for creators.",
          "params": [
            {
              "name": "inviteId",
              "description": "The unique ID for the job invitation."
            }
          ]
        }
      }
    ]
  }
}